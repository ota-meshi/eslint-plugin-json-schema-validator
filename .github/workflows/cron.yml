name: CRON
on:
  schedule:
  - cron: '0 */2 * * *'

jobs:
  update-schemastore:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
    - name: Install Packages
      run: npm install
    - name: Build
      run: npm run build
    - name: Update SchemaStore
      run: npm run update:schemastore
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
